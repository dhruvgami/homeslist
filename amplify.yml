version: 1
frontend:
  phases:
    build:
      commands:
        - cd homes
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
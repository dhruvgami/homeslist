version: 1
frontend:
  phases:
    build:
      commands:
        - cd homes
        - npm install
        - npm run build
  artifacts:
    baseDirectory: homes/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*